pipeline {
  agent any

  stages {
    stage('Destroy') {
      steps {
        sh '''
        cd files/terraform
        terraform init
        terraform destroy -auto-approve
        '''
      }
    }
  }
}
