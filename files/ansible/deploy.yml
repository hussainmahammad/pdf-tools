
---
- name: Deploy Space Canvas static assets
  hosts: space_canvas_servers
  become: true
  tasks:
    - name: Ensure nginx is installed
      ansible.builtin.package:
        name: nginx
        state: present
